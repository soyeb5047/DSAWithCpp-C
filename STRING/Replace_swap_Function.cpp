#include <iostream>
#include<string>
using namespace std;

int main()
{
    string str="programming";
    
    // str.replace(3,4,"soyeb");   //it replace from the index 3 , and it replace till 4 word with soyeb
    
    // str.erase();  // it erase the total string 
    
    // str.push_back('s');  // it adds a char in the end of the string
    
    // str.pop_back(); // it deletes one char from end 
    
    string str1="hello";
    
    str.swap(str1); // it swaps with str1
    
    cout<<str<<endl;
    
    cout<<str1<<endl;
}
